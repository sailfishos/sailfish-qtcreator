Much like 'output-dir' except that a subdirectory of the given <dir> will be used, denoted by the build target name (see 'target'). When the 'task' option is active, output directories will be further grouped by task name ('<dir>/[<task>/]<target>').
